version: 2

models:
  - name: int_educacao__aluno_frequencia
    description: >
      Camada intermediária que combina frequência com aluno (left join).
      Documente premissas: chave de join, tratamento de órfãos e filtros temporais.
    columns:
      - name: _fk_aluno_resolvido
        description: Referência ao aluno após o join (ajuste se renomear colunas no staging).
